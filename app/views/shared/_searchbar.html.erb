<%= form_tag strippers_path, method: :get do %>
  <%= text_field_tag :query,
    params[:query],
    class: "form-control text-searchbar",
    placeholder: "Where should the magic happen?"
  %>
<% end %>